services:
  - type: web
    name: song-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    rootDir: .
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 5000